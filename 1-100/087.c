// Declare an integer variable, assign it a decimal value, and print its hexadecimal representation.

#include <stdio.h>

int main()
{
    int decimal_num = 255;
    printf("O número %d em hexadecimal = %x\n", decimal_num, decimal_num);
}